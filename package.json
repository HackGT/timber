{
  "name": "timber",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build": "yarn build:client && yarn build:server",
    "build:client": "cd client && yarn install && yarn build",
    "build:server": "cd server && yarn install && yarn prisma-generate && yarn build"
  }
}
